<%= render partial: 'errors', locals: {game: @game} %>

<h1>Edit Game<h1>
<% flash.each do |type, msg| %>
  <div>
    <%= msg %>
  </div>
<% end %>

<%= render partial: 'form', locals: {game: @game} %>

<br>
<%= link_to "All Games", games_path %> | <%= link_to "All Tournaments", tournaments_path %>
<br><br>
<%= link_to "Profile", player_path(@player) %> | <%= link_to "Log out", logout_path %>